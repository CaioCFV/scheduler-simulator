import React from 'react';
import ReactDOM from 'react-dom';
import App from './pages/PRIORITY';
import './reset.css';

ReactDOM.render(
  <React.StrictMode>
      <App />
  </React.StrictMode>,
  document.getElementById('root')
);

